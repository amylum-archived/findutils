Prospectus.extra_dep('file', 'prospectus_circleci')
Prospectus.extra_dep('file', 'prospectus_pkgforge')

my_slug = 'amylum/findutils'

item do
  expected do
    url_xpath
    url 'https://mirrors.peers.community/mirrors/gnu/findutils/?C=N;O=D'
    xpath '/html/body/pre/a[6]'
    regex /^findutils-([\d.]+)\.tar.*$/
  end

  actual do
    git_tag
    regex /^(.*)-\d+$/
  end

  extend ProspectusPkgforge::DepItems
  extend ProspectusCircleci::Build.new(my_slug)
end
