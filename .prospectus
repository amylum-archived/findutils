item do
  expected do
    url_xpath
    url 'http://ftp.gnu.org/pub/gnu/findutils/?C=N;O=D'
    xpath '/html/body/table/tr[4]/td[2]/a'
    regex /^findutils-([\d.]+)\.tar\.gz\.sig$/
  end

  actual do
    git_tag
    regex /^(.*)-\d+$/
  end
end
